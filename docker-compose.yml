services:
  backend:
    build: ./server
    container_name: finsight-backend
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./client
    container_name: finsight-frontend
    ports:
      - "3000:80"
    restart: always
    depends_on:
      - backend
